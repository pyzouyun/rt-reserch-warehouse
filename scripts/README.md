# Scripts

Operational helper scripts can live here. The current prototype keeps commands in the `Makefile`:

- `make up`
- `make down`
- `make etl`
- `make import-mosaiq`

Add site-specific backup, restore, and export validation scripts here after deployment conventions are confirmed.
